# Image_classification_system
A deep learning-based image classification system that uses CNN which is trained on the CIFAR-10 dataset, capable of identifying objects across multiple categories with efficient performance.
